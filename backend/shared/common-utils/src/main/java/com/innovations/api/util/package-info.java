/**
 * Small stateless helpers shared across services (dedup-key hashing,
 * time-window arithmetic, JSON canonicalisation). Anything that touches
 * Spring belongs elsewhere.
 */
package com.innovations.api.util;
