apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Local overlay for kind / minikube dev clusters. Populated in Phase 4+.
resources:
  - ../../base

# patches:
#   - path: replicas.yaml
