apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base manifests — no cluster-specific values live here. Overlays under
# ../overlays/{local,staging,prod} patch replicas, image tags, ingress, and
# secret references.

namespace: iapi

resources:
  - namespace.yaml
  # Service manifests are added as they land in Phase 4+.
  # - postgres/
  # - redis/
  # - kafka/
  # - gateway-service/
  # - auth-service/
  # - registry-service/
  # - metrics-collector/
  # - analytics-service/
  # - dashboard-service/
  # - ml-service/

commonLabels:
  app.kubernetes.io/part-of: innovations-api
